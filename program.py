def sum(a: int, b: int) -> int:
    """_summary_

    Args:
        a (int): It will take the value one
        b (int): It will take the value one

    Returns:
        int: Sum of both values
    """
    a = 10
    b = 10
    # Adding two values
    c = a + b
    return c
